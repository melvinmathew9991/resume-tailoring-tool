"""Streamlit frontend.

Kept outside ``src/resume_tailor`` because it is a separate deployable: the API
container does not need Streamlit installed, and the UI container does not need
a TeX engine.
"""
