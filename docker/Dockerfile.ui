# UI image: Streamlit only.
#
# Deliberately has no TeX toolchain. The frontend never compiles anything, so
# giving it a 1 GB LaTeX install would be a gigabyte of attack surface for no
# capability.

FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

RUN apt-get update && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY pyproject.toml README.md ./
COPY src/ ./src/
COPY ui/ ./ui/
RUN pip install --no-cache-dir ".[ui]"

RUN useradd --create-home --uid 10002 uiuser \
    && chown -R uiuser:uiuser /app
USER uiuser

ENV RT_UI_MODE=http \
    RT_ENVIRONMENT=docker \
    RT_LOG_JSON=true \
    STREAMLIT_SERVER_HEADLESS=true \
    STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

EXPOSE 8501

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 \
    CMD curl -fsS http://127.0.0.1:8501/_stcore/health || exit 1

CMD ["streamlit", "run", "ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
